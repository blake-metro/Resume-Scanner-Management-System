package groupsix.resume;

import javafx.application.Application;

/**
 * Alternate launcher class for starting the program.  Run from this file if running from
 * ResumeMain.java doesn't work
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(ResumeMain.class, args);
    }
}
